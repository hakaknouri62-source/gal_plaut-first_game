using UnityEngine;

/*
 * you can use the movement funtions nouri!!!!
 * * Notice there are no public/private modifiers here, and no curly brackets. 
 * Just the function signatures ending in a semicolon! 
 * * Moves the entity left or right.
 * directionInput: The horizontal direction to move (usually between -1 and 1).
 * moveSpeed: How fast the entity should travel.
 **** void MoveHorizontal(float directionInput, float moveSpeed);
 * * Applies an instant upward velocity to the entity.
 * jumpForce: The upward power of the jump.
 **** void Jump(float jumpForce);
 * * Checks if the entity is currently touching a ground surface.
 * Returns true if grounded, false if in the air.
 **** bool isStandingOnGround();
 */
 // you can see in playerMovement.cs how to call those funtions
public class enemieMovement : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}